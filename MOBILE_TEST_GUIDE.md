# 移动端真机测试指南

## 📱 快速开始

本指南帮助您在真实手机上测试易宿酒店预订平台移动端。

**您的电脑IP地址**: `192.168.2.20`

---

## ✅ 前置条件

1. **手机和电脑连接同一个WiFi**
2. **已安装 Node.js** (v16+)
3. **已安装项目依赖**

---

## 🚀 测试步骤

### 步骤1: 启动后端服务

打开**终端1**，执行：

```bash
cd server
npm start
```

**期望输出**：
```
=================================
  易宿酒店预订平台 - 后端服务
=================================
🚀 服务器运行在 http://localhost:3000
🌐 局域网访问: http://192.168.2.20:3000
📊 API地址: http://localhost:3000/api
=================================
💡 提示: 确保手机和电脑在同一WiFi网络
=================================
```

**保持此终端运行，不要关闭！**

---

### 步骤2: 启动移动端

打开**终端2**（新窗口），执行：

```bash
cd client-mobile
npm start
```

**期望输出**：
```
Starting the development server...

Compiled successfully!

You can now view easystay-mobile in the browser.

  Local:            http://localhost:3001
  On Your Network:  http://192.168.2.20:3001
```

⚠️ **注意**：如果3001端口被占用，会自动使用其他端口（如3006），请以实际显示的端口为准。

**保持此终端运行，不要关闭！**

---

### 步骤3: 手机访问

#### 方法A: 浏览器直接访问

1. 打开手机浏览器（Safari/Chrome）
2. 输入地址（**根据实际端口修改**）：
   - 如果端口是3001：`http://192.168.2.20:3001`
   - 如果端口是3006：`http://192.168.2.20:3006`
   - 其他端口以此类推
3. 即可看到移动端应用

#### 方法B: 二维码扫描（推荐）

在电脑终端执行（**将3006替换为实际端口**）：

```bash
npx qrcode-terminal http://192.168.2.20:3006
```

然后手机扫描二维码即可访问。

---

## 🔧 常见问题

### 问题1: 手机无法访问页面

**症状**: 手机浏览器显示"无法访问此网站"

**解决方案**:

1. **检查IP地址**
   ```bash
   ipconfig
   ```
   确认IPv4地址是 `192.168.2.20`

2. **关闭Windows防火墙**（临时）
   - 控制面板 → Windows Defender 防火墙 → 启用或关闭防火墙
   - 选择"关闭Windows Defender防火墙"

3. **检查服务是否运行**
   - 确认两个终端都没有报错
   - 确认看到 `Compiled successfully!`

### 问题2: 页面加载但数据不显示

**症状**: 能看到界面，但酒店列表为空

**解决方案**:

1. **检查后端是否运行**
   - 确认终端1显示 `服务器运行在 http://localhost:3000`

2. **检查API配置**
   
   查看 `client-mobile/.env` 文件：
   ```bash
   REACT_APP_API_URL=http://192.168.2.20:3000/api
   ```

3. **重启移动端服务**
   ```bash
   cd client-mobile
   npm start
   ```

### 问题3: 手机能访问但电脑不能

**症状**: 手机正常，但电脑浏览器访问 `localhost:3001` 失败

**解决方案**:

这是正常的！配置了局域网IP后，请使用：
- 电脑访问：`http://192.168.2.20:3001`
- 手机访问：`http://192.168.2.20:3001`

---

## 📋 测试检查清单

### 基础功能测试
- [ ] 搜索页面正常显示
- [ ] 酒店列表加载正常
- [ ] 酒店详情页正常显示
- [ ] 图片加载正常
- [ ] 页面滚动流畅

### 交互测试
- [ ] 点击酒店卡片进入详情
- [ ] 搜索功能正常
- [ ] 返回按钮正常
- [ ] 底部导航正常

### 响应式测试
- [ ] iPhone 尺寸显示正常
- [ ] Android 尺寸显示正常
- [ ] 横屏/竖屏切换正常

---

## 🔄 重新配置IP地址

如果您的IP地址发生变化（如切换WiFi），需要重新配置：

### 1. 查看新IP地址

```bash
ipconfig
```

找到 `IPv4 地址`，例如：`192.168.2.100`

### 2. 修改环境变量

编辑 `client-mobile/.env`：

```bash
# 修改为新的IP地址
REACT_APP_API_URL=http://192.168.2.100:3000/api
```

### 3. 修改后端配置

编辑 `server/app.js`，找到：

```javascript
console.log(`🌐 局域网访问: http://192.168.2.20:${PORT}`);
```

改为新的IP地址。

### 4. 重启所有服务

关闭所有终端，重新启动后端和移动端。

---

## 🛠️ 高级配置

### 配置环境变量

创建 `client-mobile/.env.local`（不会被提交到git）：

```bash
# 开发环境
REACT_APP_API_URL=http://192.168.2.20:3000/api

# 测试环境
# REACT_APP_API_URL=http://192.168.2.20:3000/api

# 生产环境
# REACT_APP_API_URL=https://your-domain.com/api
```

### 使用HTTPS（高级）

如需HTTPS访问，可使用mkcert：

```bash
# 安装mkcert
npm install -g mkcert

# 生成证书
mkcert localhost 192.168.2.20

# 配置React使用HTTPS
HTTPS=true npm start
```

---

## 📞 获取帮助

如果以上步骤无法解决问题：

1. **检查终端错误信息**
2. **确认手机和电脑在同一WiFi**
3. **尝试关闭防火墙**
4. **重启电脑后重试**

---

**祝您测试顺利！** 🎉
