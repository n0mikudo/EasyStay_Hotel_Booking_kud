# EasyStay 项目部署指南

## 服务器信息
- 服务器 IP: 81.71.15.150
- 域名: easystay4u.site（已配置 HTTPS）
- 项目目录: /root/hotel/EasyStay_Hotel_Booking_kud
- 服务器系统: Linux (Ubuntu)

---

## 一、前置准备

### 1.1 服务器环境要求
- Node.js 16+
- Nginx 1.18+
- PM2 (进程管理器)

### 1.2 安装 Node.js
```bash
# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# CentOS/RHEL
curl -fsSL https://rpm.nodesource.com/setup_18.x | sudo bash -
sudo yum install -y nodejs
```

### 1.3 安装 Nginx
```bash
# Ubuntu/Debian
sudo apt-get install -y nginx

# CentOS/RHEL
sudo yum install -y nginx
```

### 1.4 安装 PM2
```bash
sudo npm install -g pm2
```

---

## 二、上传项目文件到服务器

### 2.1 方式一：使用 SCP 上传
```bash
# 在本地电脑执行
scp -r EasyStay_Project root@81.71.15.150:/opt/
```

### 2.2 方式二：使用 Git (推荐)
```bash
# 在服务器上执行
cd /opt
git clone <你的仓库地址> EasyStay_Project
```

---

## 三、后端部署

### 3.1 安装依赖
```bash
cd /root/hotel/EasyStay_Hotel_Booking_kud/server
npm install
```

### 3.2 配置环境变量
```bash
cd /root/hotel/EasyStay_Hotel_Booking_kud/server
cp .env.example .env
nano .env
```

编辑 `.env` 文件：
```env
# 服务器端口
PORT=3000

# 允许的CORS源（生产环境请配置具体域名）
CORS_ORIGIN=*

# 数据目录
DATA_DIR=./data
```

### 3.3 使用 PM2 统一启动所有服务（推荐）
```bash
cd /root/hotel/EasyStay_Hotel_Booking_kud

# 使用 pm2.config.js 统一启动后端、管理端、移动端
pm2 start pm2.config.js

# 保存 PM2 配置（开机自启）
pm2 save
pm2 startup
```

### 3.4 单独启动后端服务（可选）
```bash
cd /root/hotel/EasyStay_Hotel_Booking_kud/server
pm2 start app.js --name easystay-server
pm2 save
pm2 startup
```

### 3.5 查看服务状态
```bash
pm2 status
pm2 logs easystay-server
pm2 logs easystay-admin
pm2 logs easystay-mobile
```

---

## 四、前端部署

### 方式一：使用 PM2 开发模式运行（已配置，推荐）

前端已通过 Nginx 反向代理 + HTTPS 部署，访问地址：
- 管理端: https://easystay4u.site
- 移动端: https://easystay4u.site/mobile/
- IP 直连（备用）: http://81.71.15.150

### 方式二：生产环境构建（可选）

#### 4.1 构建管理端
```bash
cd /root/hotel/EasyStay_Hotel_Booking_kud/admin-pc
npm install

# 创建生产环境配置文件
cat > .env.production << 'EOF'
REACT_APP_API_URL=http://81.71.15.150:3000/api
EOF

# 构建
npm run build
```

#### 4.2 构建移动端
```bash
cd /root/hotel/EasyStay_Hotel_Booking_kud/client-mobile
npm install

# 创建生产环境配置文件
cat > .env.production << 'EOF'
REACT_APP_API_URL=http://81.71.15.150:3000/api
EOF

# 构建
npm run build
```

---

## 五、Nginx + HTTPS 配置（已部署）

当前项目使用 Nginx 反向代理 + Let's Encrypt HTTPS 证书，配置文件位于 `/etc/nginx/sites-enabled/default`。

### 5.1 域名与 SSL 证书

- **域名**：`easystay4u.site`（阿里云注册，指向 81.71.15.150）
- **SSL 证书**：Let's Encrypt，通过 HTTP-01 验证申请
- **证书路径**：`/etc/letsencrypt/live/easystay4u.site/`
- **自动续期**：certbot 定时任务，到期前 30 天自动续期

### 5.2 Nginx 配置结构

```nginx
# HTTP → HTTPS 重定向（域名访问）
server {
    listen 80 default_server;
    server_name easystay4u.site;
    return 301 https://$host$request_uri;
}

# IP 直连保留 HTTP 访问（兼容性）
server {
    listen 80;
    server_name 81.71.15.150;
    # ... 完整的 location 配置（管理端/移动端/API）
}

# HTTPS 主服务（TLS 1.2/1.3 + HTTP/2）
server {
    listen 443 ssl http2;
    server_name easystay4u.site;
    ssl_certificate /etc/letsencrypt/live/easystay4u.site/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/easystay4u.site/privkey.pem;
    # ... 完整的 location 配置（管理端/移动端/API/SSE代理）
}
```

### 5.3 常用维护命令

```bash
# 测试 Nginx 配置
sudo nginx -t

# 重载 Nginx
sudo systemctl reload nginx

# 手动续期证书（通常不需要，自动续期）
sudo certbot renew

# 查看证书信息
sudo certbot certificates
```

---

## 六、防火墙配置

### 6.1 开放端口
```bash
# Ubuntu/Debian (UFW)
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable

# CentOS/RHEL (Firewalld)
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

---

## 七、访问应用

已通过 Nginx + HTTPS 部署，访问地址：

- **管理端**: https://easystay4u.site
- **移动端**: https://easystay4u.site/mobile/
- **API 接口**: https://easystay4u.site/api
- **IP 直连（备用）**: http://81.71.15.150

### 测试账号
- **管理员**: admin / admin123
- **商户**: merchant1 / merchant123

---

## 八、日常维护

### 8.1 查看服务状态
```bash
cd /root/hotel/EasyStay_Hotel_Booking_kud

# 查看所有服务状态
pm2 status

# 查看服务日志
pm2 logs easystay-server
pm2 logs easystay-admin
pm2 logs easystay-mobile

# 查看所有日志（最后 50 行）
pm2 logs --lines 50
```

### 8.2 重启服务
```bash
# 重启所有服务
pm2 restart all

# 重启单个服务
pm2 restart easystay-server
pm2 restart easystay-admin
pm2 restart easystay-mobile
```

### 8.3 停止/删除服务
```bash
# 停止所有服务
pm2 stop all

# 停止单个服务
pm2 stop easystay-server

# 删除所有服务
pm2 delete all

# 删除单个服务
pm2 delete easystay-server
```

### 8.4 备份数据
```bash
# 备份数据目录
cd /root/hotel/EasyStay_Hotel_Booking_kud
tar -czf data_backup_$(date +%Y%m%d).tar.gz server/data/
```

---

## 九、常见问题

### 9.1 端口被占用
```bash
# 查看所有相关端口占用
netstat -tuln 2>/dev/null | grep -E ":(3000|3001|3011)"
# 或
ss -tuln 2>/dev/null | grep -E ":(3000|3001|3011)"

# 查看并终止占用进程
kill -9 $(lsof -ti :3000 2>/dev/null) 2>/dev/null || true
kill -9 $(lsof -ti :3001 2>/dev/null) 2>/dev/null || true
kill -9 $(lsof -ti :3011 2>/dev/null) 2>/dev/null || true
```

### 9.2 权限问题
```bash
# 修改项目目录权限
cd /root/hotel/EasyStay_Hotel_Booking_kud
sudo chown -R $(whoami):$(whoami) .
sudo chmod -R 755 .
```

### 9.3 PM2 服务异常
```bash
# 查看错误日志
pm2 logs easystay-server --lines 100
pm2 logs easystay-admin --lines 100
pm2 logs easystay-mobile --lines 100

# 重启服务
pm2 restart all

# 如果持续错误，删除后重新启动
pm2 delete all
pm2 start pm2.config.js
```

### 9.4 依赖缺失
```bash
# 重新安装依赖
cd /root/hotel/EasyStay_Hotel_Booking_kud/server
rm -rf node_modules package-lock.json
npm install

cd ../admin-pc
rm -rf node_modules package-lock.json
npm install

cd ../client-mobile
rm -rf node_modules package-lock.json
npm install

# 重启服务
cd ..
pm2 restart all
```
