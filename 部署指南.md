# EasyStay 项目部署指南

## 服务器信息
- 服务器 IP: 81.71.15.150
- 假设服务器使用 Linux 系统（Ubuntu/CentOS）

---

## 一、前置准备

### 1.1 服务器环境要求
- Node.js 16+
- Nginx 1.18+
- PM2 (进程管理器)

### 1.2 安装 Node.js
```bash
# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# CentOS/RHEL
curl -fsSL https://rpm.nodesource.com/setup_18.x | sudo bash -
sudo yum install -y nodejs
```

### 1.3 安装 Nginx
```bash
# Ubuntu/Debian
sudo apt-get install -y nginx

# CentOS/RHEL
sudo yum install -y nginx
```

### 1.4 安装 PM2
```bash
sudo npm install -g pm2
```

---

## 二、上传项目文件到服务器

### 2.1 方式一：使用 SCP 上传
```bash
# 在本地电脑执行
scp -r EasyStay_Project root@81.71.15.150:/opt/
```

### 2.2 方式二：使用 Git (推荐)
```bash
# 在服务器上执行
cd /opt
git clone <你的仓库地址> EasyStay_Project
```

---

## 三、后端部署

### 3.1 安装依赖
```bash
cd /opt/EasyStay_Project/server
npm install
```

### 3.2 配置环境变量
```bash
cp .env.example .env
nano .env
```

编辑 `.env` 文件：
```env
# 服务器端口
PORT=3000

# 允许的CORS源（生产环境请配置具体域名）
CORS_ORIGIN=*

# 数据目录
DATA_DIR=./data
```

### 3.3 使用 PM2 启动后端服务
```bash
cd /opt/EasyStay_Project/server
pm2 start app.js --name easystay-server
pm2 save
pm2 startup
```

### 3.4 查看服务状态
```bash
pm2 status
pm2 logs easystay-server
```

---

## 四、前端部署

### 4.1 构建管理端
```bash
cd /opt/EasyStay_Project/admin-pc
npm install

# 创建生产环境配置文件
cat > .env.production << 'EOF'
REACT_APP_API_URL=http://81.71.15.150/api
EOF

# 构建
npm run build
```

### 4.2 构建移动端
```bash
cd /opt/EasyStay_Project/client-mobile
npm install

# 创建生产环境配置文件
cat > .env.production << 'EOF'
REACT_APP_API_URL=http://81.71.15.150/api
EOF

# 构建
npm run build
```

---

## 五、Nginx 配置

### 5.1 创建 Nginx 配置文件
```bash
sudo nano /etc/nginx/sites-available/easystay
```

### 5.2 配置内容
```nginx
server {
    listen 80;
    server_name 81.71.15.150;

    # 管理端
    location / {
        root /opt/EasyStay_Project/admin-pc/build;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # 移动端 (可选：使用子域名或子路径)
    location /mobile/ {
        alias /opt/EasyStay_Project/client-mobile/build/;
        index index.html;
        try_files $uri $uri/ /mobile/index.html;
    }

    # 后端 API
    location /api/ {
        proxy_pass http://localhost:3000/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

### 5.3 启用配置
```bash
# 创建软链接
sudo ln -s /etc/nginx/sites-available/easystay /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重启 Nginx
sudo systemctl restart nginx
```

---

## 六、防火墙配置

### 6.1 开放端口
```bash
# Ubuntu/Debian (UFW)
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable

# CentOS/RHEL (Firewalld)
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

---

## 七、访问应用

部署完成后，可以通过以下地址访问：

- 管理端: http://81.71.15.150
- 移动端: http://81.71.15.150/mobile/ (如果配置了)
- API 接口: http://81.71.15.150/api/

---

## 八、日常维护

### 8.1 查看日志
```bash
# 后端日志
pm2 logs easystay-server

# Nginx 日志
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log
```

### 8.2 重启服务
```bash
# 重启后端
pm2 restart easystay-server

# 重启 Nginx
sudo systemctl restart nginx
```

### 8.3 备份数据
```bash
# 备份数据目录
cd /opt/EasyStay_Project
tar -czf data_backup_$(date +%Y%m%d).tar.gz server/data/
```

---

## 九、常见问题

### 9.1 端口被占用
```bash
# 查看端口占用
sudo netstat -tlnp | grep :3000

# 杀死占用进程
sudo kill -9 <PID>
```

### 9.2 权限问题
```bash
# 修改项目目录权限
sudo chown -R www-data:www-data /opt/EasyStay_Project
sudo chmod -R 755 /opt/EasyStay_Project
```

### 9.3 SELinux 问题 (CentOS)
```bash
# 临时关闭 SELinux
sudo setenforce 0

# 或配置 SELinux 规则
sudo setsebool -P httpd_can_network_connect 1
```
